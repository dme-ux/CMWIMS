import { NextRequest,NextResponse } from "next/server";import { prisma } from "@/lib/prisma";import { getSession } from "@/lib/auth/session";import { can } from "@/lib/auth/rbac";import { WORKSHOP_STATUSES } from "@/lib/workshop";
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await getSession();if(!s||!can(s.role,'workshop.manage'))return NextResponse.json({error:'No permission'},{status:403});const b=await req.json();const old=await prisma.workshopJob.findUnique({where:{id}});if(!old)return NextResponse.json({error:'Job card not found.'},{status:404});const data:any={};if(b.status){if(!(WORKSHOP_STATUSES as readonly string[]).includes(b.status))return NextResponse.json({error:'Invalid status.'},{status:400});data.status=b.status}if(b.remark?.trim()){const stamp=new Date().toLocaleString('en-IN',{timeZone:'Asia/Kolkata'});data.remarks=[old.remarks,`${stamp}: ${b.remark.trim()}`].filter(Boolean).join(' | ')}const job=await prisma.workshopJob.update({where:{id},data});await prisma.auditLog.create({data:{userId:s.id,action:'JOB_UPDATE',entity:'WorkshopJob',entityId:id,detail:`${old.jobNumber}${b.status?` -> ${b.status}`:''}${b.remark?` · ${b.remark}`:''}`}});return NextResponse.json({job})}
